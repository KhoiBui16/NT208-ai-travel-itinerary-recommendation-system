# ── API ──────────────────────────────────────────────
# Backend URL. Default is http://localhost:8000 when running BE locally.
VITE_API_URL=http://localhost:8000

# Goong Maps tiles API key (browser-side map rendering). Get one at https://goong.io/dashboard.
VITE_GOONG_MAP_API_KEY=
